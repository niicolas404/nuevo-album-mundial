=== Etapa 1 ===
Prompt: no hubo uso de IA.
Resultado:
Utilidad: 

=== Etapa 2 ===
Prompt: Vamos a crear un componente reutilizable. Para empezar, se debe considerar la información en #file:stickers.js para crear el componente. Los props que debe recibir son "number, name, group, status". Además, se debe poder mostrar la información de la figurita de forma visual (numero, nombre, grupo). Aplica estilos básicos según el status: quiero un fondo verde para "tengo" amarillo para "repetida" Y gris para "falta".

en #file:App.jsx renderiza al menos 10 figuritas usando el componente, pasando los datos desde el array importado.
en #file:App.jsx borra todo lo referente a la pagina por defecto. Es importante que solo se muestre lo que estoy pidiendo.

prompt 2: veo que las figuritas no cuentan con toda la información que pedí. Necesito que en todas se muestre número, nombre y grupo.


Resultado:El primer resultado fue algo deficiente; las figuritas no mostraban toda la información, por lo qu etuve que recurrir a una segunda instrcción.
Utilidad: parcialmente util.

=== Etapa 3 ===
Prompt: vamos a crear la interacción de usuario. Necesito que en #file:App.jsx uses "useState" para manejar los estados de las figuritas { [id]: "falta" | "tengo" | "repetida" }

Además, todas deben iniciar en el estado "falta". Debes crear una función handleStatusChange(id) que cambie el estado de la figura en el siguiente orden: falta > tengo > repetida > falta.

pasa la función como props a StickerCard y ejecutala al hacer click en la tarjeta. Es importante que el componente muestre visualmente el estado y que cambie al hacer click.
Resultado: cumplió de manera eficiente las instruciones. 
Utilidad: Fui muy util.

=== Etapa 4 ===
Prompt: vamos a añadir filtros y búsquedas. para empezar, agregar un campo de búsqueda de texto con "useState" y que filtre las figurtas por nombre y número. También agrega botones o un selector para filtrar "todas, "tengo", "repetidas". Necesito que apliques ambos filtros simultaneamente sobre el array antes de renderizar las tarjetas. Por último, muestra el total de figuras visibles según el filtro activo.
prompt 2: agrega "falta" al filtro de búsquedas y hazlo funcionar siguiendo los mismos parámetros. Además, haz que las letras sean visiables antes de yo hacer click sobre el filtro de búsqueda.
Resultado:cumplió con los filtros, pero las letras no son legibles a pesar de haberle pedido lo contrario.
Utilidad: parcialmente util.


=== Etapa 5 ===
Prompt: 
Resultado:
Utilidad: 

